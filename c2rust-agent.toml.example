# C2RustAgent LLM 配置文件示例
# 
# 配置文件位置（按优先级从高到低）：
# 1. 环境变量（如 C2RUST_AGENT_API_KEY）
# 2. 用户配置文件：
#    - Linux/macOS: ~/.config/c2rust-agent/config.toml
#    - Windows: %APPDATA%\c2rust-agent\config.toml
# 3. 项目配置文件：./c2rust-agent.toml
# 4. 默认值

# ========================================
# API 提供商设置
# ========================================

# API 提供商类型：openai, azure, custom
provider = "openai"

# OpenAI API Key
# 也可以通过环境变量 OPENAI_API_KEY 或 C2RUST_AGENT_API_KEY 设置
# 获取方式：https://platform.openai.com/api-keys
api_key = "sk-your-api-key-here"

# 自定义 API URL（可选）
# 用于以下场景：
# - 使用代理或中转服务
# - 使用 Azure OpenAI
# - 使用 OpenAI 兼容的本地模型（如 LocalAI、Ollama）
#
# 示例：
# api_url = "https://api.openai.com/v1"                    # OpenAI 官方（默认）
# api_url = "https://your-proxy.com/v1"                    # 自定义代理
# api_url = "https://your-endpoint.openai.azure.com"       # Azure OpenAI
# api_url = "http://localhost:8080/v1"                     # 本地模型

# ========================================
# 模型参数
# ========================================

# 温度参数（0.0-2.0）
# - 0.0：输出最确定，适合生成文档和代码注释
# - 1.0：平衡创造性和确定性
# - 2.0：最大随机性
# 推荐：0.3（稳定且质量高）
temperature = 0.3

# 最大生成 token 数
# 建议：1000-2000 之间
# 注意：过大会增加成本，过小可能截断响应
max_tokens = 1000

# ========================================
# 开发/测试选项
# ========================================

# 是否使用 Mock 模式（用于测试，不实际调用 API）
# 也可以通过环境变量 USE_MOCK_LLM=true 设置
use_mock = false
